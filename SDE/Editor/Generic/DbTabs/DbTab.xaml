<TabItem x:Class="SDE.Editor.Generic.DbTabs.DbTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
         xmlns:ListView="clr-namespace:TokeiLibrary.WPF.Styles.ListView;assembly=TokeiLibrary"
         xmlns:TokeiLibrary="clr-namespace:TokeiLibrary;assembly=TokeiLibrary"
         xmlns:WPF="clr-namespace:TokeiLibrary.WPF;assembly=TokeiLibrary"
         xmlns:WpfBugFix="clr-namespace:TokeiLibrary.WpfBugFix;assembly=TokeiLibrary"
         xmlns:Styles="clr-namespace:TokeiLibrary.WPF.Styles;assembly=TokeiLibrary"
         xmlns:controls="clr-namespace:SDE.View.Controls"
         mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid x:Name="_viewGrid" Background="{DynamicResource TabItemBackground}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
		<Grid Focusable="false" TextBlock.Foreground="{DynamicResource TextForeground}">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <controls:DbSearchPanel x:Name="_dbSearchPanel" />
			<WpfBugFix:RangeListView Background="{DynamicResource TabItemBackground}" TextSearch.TextPath="[0]" FocusVisualStyle="{x:Null}" BorderThickness="0" Grid.Row="1" ListView:ListViewExtensions.IsGridSortable="True" Name="_listView" VirtualizingStackPanel.IsVirtualizing="True" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
				<WpfBugFix:RangeListView.ContextMenu>
					<ContextMenu>
					</ContextMenu>
				</WpfBugFix:RangeListView.ContextMenu>
			</WpfBugFix:RangeListView>
        </Grid>
        <GridSplitter Width="2" Grid.Column="1" HorizontalAlignment="Left" Background="{x:Static TokeiLibrary:Configuration.UIGridBackground}" />
    </Grid>
</TabItem>
