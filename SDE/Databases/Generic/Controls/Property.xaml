<UserControl x:Class="SDE.Databases.Generic.Controls.Property"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
		xmlns:local="clr-namespace:SDE.Databases.Generic.Controls"
		xmlns:Tmc="clr-namespace:SDE.Editor.Generic.DbTabs"
		mc:Ignorable="d" 
		d:DesignWidth="200"
		x:Name="Root">
	<Grid x:Name="_grid">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" SharedSizeGroup="Label" />
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>

		<TextBlock
			Margin="0 3 6 3"
			VerticalAlignment="Center"
			x:Name="_tbPropertyLabel"
			Text="{Binding Label, ElementName=Root}"
			ToolTip="{Binding ToolTip, ElementName=Root}"
			Visibility="Collapsed"
		/>
		<ContentPresenter Grid.Column="1" Content="{Binding Editor, ElementName=Root}" />
		<ItemsControl Grid.Column="2" ItemsSource="{Binding Extras, ElementName=Root}">
			<ItemsControl.ItemsPanel>
				<ItemsPanelTemplate>
					<StackPanel Orientation="Horizontal"/>
				</ItemsPanelTemplate>
			</ItemsControl.ItemsPanel>
		</ItemsControl>
	</Grid>
</UserControl>
