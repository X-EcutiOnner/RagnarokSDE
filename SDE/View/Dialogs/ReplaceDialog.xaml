<Styles:TkWindow x:Class="SDE.View.Dialogs.ReplaceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Styles="clr-namespace:TokeiLibrary.WPF.Styles;assembly=TokeiLibrary"
        xmlns:TokeiLibrary="clr-namespace:TokeiLibrary;assembly=TokeiLibrary"
        xmlns:ListView="clr-namespace:TokeiLibrary.WPF.Styles.ListView;assembly=TokeiLibrary"
		SnapsToDevicePixels="True" UseLayoutRounding="True"
		Width="250"
        Background="{StaticResource TabItemBackground}" Foreground="{StaticResource TextForeground}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid Name="_jobGrid" HorizontalAlignment="Left">
            <Grid.RowDefinitions>
                <RowDefinition />
            </Grid.RowDefinitions>
            <StackPanel>
                <Label Content="Replace all values..." FontStyle="Italic" VerticalAlignment="Center" />
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="auto" />
						<ColumnDefinition Width="120" />
					</Grid.ColumnDefinitions>
					<Grid.RowDefinitions>
						<RowDefinition Height="auto" />
						<RowDefinition Height="auto" />
						<RowDefinition />
					</Grid.RowDefinitions>
					<TextBlock Text="Attribute" VerticalAlignment="Center" Width="70" Margin="3" Grid.Row="0" />
					<TextBlock Text="New value" VerticalAlignment="Center" Width="70" Margin="3" Grid.Row="1" />
					<ComboBox Name="_cbField" SelectionChanged="_cbField_SelectionChanged" VerticalAlignment="Center" Margin="3" Grid.Column="1" Height="22" />
					<Grid Width="120" Grid.Column="1" Grid.Row="1">
						<TextBox Name="_stringNewValue" Margin="3" Height="22" VerticalContentAlignment="Center" Visibility="Collapsed" />
						<CheckBox x:Name="_boolNewValue" VerticalAlignment="Center" Margin="3" Visibility="Collapsed" />
						<ComboBox Name="_enumNewValue" Height="22" Margin="3" Visibility="Collapsed" />
					</Grid>
				</Grid>
            </StackPanel>
        </Grid>
        <Grid TokeiLibrary:WpfProperties.IsDraggable="True" Grid.Row="1" Height="49" Background="{DynamicResource UIDialogBackground}">
            <Grid Margin="0 0 3 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <DockPanel Grid.Column="2">
                    <Button Name="_buttonOk" Click="_buttonOk_Click" Content="Replace" Height="25" Margin="3" MinWidth="100" Grid.Column="2" />
                    <Button Name="_buttonCancel" Click="_buttonCancel_Click" Content="Cancel" Height="25" Margin="3" MinWidth="100" Grid.Column="3" />
                </DockPanel>
            </Grid>
        </Grid>
    </Grid>
</Styles:TkWindow>
